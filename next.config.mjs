/** @type {import('next').NextConfig} */
const nextConfig = {
    // Your other Next.js configurations go here.
    // For example, if you had an 'images' object previously:
    images: {
      domains: ['localhost'],
    },
  };
  
  // Changed from module.exports to export default for ES module scope
  export default nextConfig;
